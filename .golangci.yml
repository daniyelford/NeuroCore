version: "1"

run:
  timeout: 5m

linters:
  enable:
    - govet
    - staticcheck
    - errcheck
    - ineffassign
    - unused
    - gosimple
    - gofmt
    - misspell

issues:
  max-issues-per-linter: 0
  max-same-issues: 0